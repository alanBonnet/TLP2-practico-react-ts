import React from 'react'
import Carousel from '../components/Carousel'

interface Props {
    
}

export const Home = (props: Props) => {
    return (
        <>
            <Carousel/>
        </>
    )
}
